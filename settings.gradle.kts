pluginManagement {
    repositories {
        maven("https://www.jetbrains.com/intellij-repository/releases")
        maven("https://cache-redirector.jetbrains.com/plugins.jetbrains.com/maven")
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "IBooks"
